package fixtures.boiler.steam.detector.controller {
	import flash.events.Event;

	/**
	 * A Class that detects as a controller because of it's namespace
	 */
	public class ControllerByNamespace {
		public function anAction(event:Event):void {
		}
	}
}
