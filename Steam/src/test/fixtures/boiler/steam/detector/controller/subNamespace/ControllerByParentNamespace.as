package fixtures.boiler.steam.detector.controller.subNamespace {
	import flash.events.Event;

	/**
	 * A Class that detects as a controller because of a parent namespace
	 */
	public class ControllerByParentNamespace {
		public function anAction(event:Event):void {
		}
	}
}
