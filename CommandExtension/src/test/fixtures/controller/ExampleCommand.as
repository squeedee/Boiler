package fixtures.controller {

	/**
	 * Minimum example of a command.
	 * It's in the controller namespace.
	 * It's name ends in 'Command'
	 * It has a public 'execute' function which accepts a Simple event. (See steam documentation for definition of
	 * a Simple event)
	 */
	public class ExampleCommand {

		public function execute(event:ExampleEvent):void {

		}
	}
}
